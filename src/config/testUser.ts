/**
 * Test User Configuration
 * 
 * This is a dedicated test account used by admins in the Test Lab
 * to simulate friend requests, follows, and notifications.
 * 
 * The test user is hidden from normal discovery (search, suggestions, etc.)
 * via the `is_test = true` flag on their profile.
 */

// Placeholder UUID - this will be set after creating the test user in production
// For now, we'll use a placeholder that gets created dynamically if needed
export const TEST_USER_ID = 'clbhouz-test-user';
export const TEST_USER_USERNAME = 'clbhouz_test';
export const TEST_USER_DISPLAY_NAME = 'Clbhouz Test';
export const TEST_USER_EMAIL = 'test+clbhouz@clbhouz.com';
