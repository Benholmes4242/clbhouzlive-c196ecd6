/**
 * Tour Hub primary tab identifiers. Used by the URL `?tab=` param, the nav
 * overlay, and (post-rollout) the canonical fixed shell.
 */
export type TourHubTab =
  | 'overview'
  | 'news'
  | 'live'
  | 'schedule'
  | 'players'
  | 'leaderboards';
