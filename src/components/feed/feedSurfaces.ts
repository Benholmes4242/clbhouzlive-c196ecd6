import { FEED_CARD_SURFACE, IMMERSIVE_FEED_CANVAS } from '@/lib/tokens/surfaces';

/** Page surface revealed by the transparent gaps between feed items. */
export const CANVAS = IMMERSIVE_FEED_CANVAS;

/** Shared surface for member post cards and editorial Wire slides. */
export const SLAB = FEED_CARD_SURFACE;