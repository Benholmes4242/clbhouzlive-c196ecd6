
import React from 'react';

const FloatingPostButton = () => {
  // This component is now disabled since we have the Post button in the navigation bar
  return null;
};

export default FloatingPostButton;
