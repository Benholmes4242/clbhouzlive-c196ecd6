import AuthMonitoringDashboard from "@/components/admin/AuthMonitoringDashboard";

/**
 * Auth Monitoring Page
 * 
 * Note: Header is rendered inside AuthMonitoringDashboard via AuthMonitoringHeader component.
 * Do NOT add a duplicate header here.
 */
export function AuthMonitoringPage() {
  return <AuthMonitoringDashboard />;
}

export default AuthMonitoringPage;
