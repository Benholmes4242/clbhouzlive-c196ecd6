# Discover World Class Amendment 1

## Scope
Apply the amendment to the live Discover page only, except for the app-wide dark under-par token consolidation. Preserve the two unchecked hardcoded `#EF4444` surfaces and make no layout, query, or hue additions.

## Implementation
- Repoint the canonical dark under-par score token and dark scorecard birdie fill to the existing bright analytical red `#FF6B60`, documenting the four-value consolidation where the tokens live.
- Collapse Discover text into two local tiers: factual content at full white; pure slot labels at one quiet white tier of 0.70. Avoid editing shared ink tokens that would affect other pages.
- Change hero selection to the most recent non-plain round, except that the most recent ace/albatross in the seven-day window overrides every non-rarity. Continue using the rounds and batched hole-shape data already fetched by the section.
- Add focused tests for recency, rarity hold, all-plain suppression, and dark-red equivalence.

## Verification and report
- Validate the hero figure and 17px birdie marker visually and calculate WCAG contrast for `#FF6B60` on the relevant dark grounds, including white numeral contrast on the red fill.
- Inventory both hardcoded `#EF4444` renderers without changing them.
- Produce the complete Discover text-role before/after list and identify every shared token intentionally left unchanged.
- Derive the number of distinct historical hero holders over four weeks from available data; if production history is unavailable to the client query, report the exact limitation rather than inventing a number.
- Verify the authenticated Discover page at the current mobile viewport and run focused tests.
